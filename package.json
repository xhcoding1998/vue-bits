{
  "name": "vue-bits-cn-atlas",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "sync:catalog": "node scripts/generate-catalog.mjs",
    "dev": "vite --host 127.0.0.1",
    "build": "vue-tsc --noEmit -p tsconfig.app.json && vite build && node scripts/prepare-worker.mjs",
    "preview": "vite preview --host 127.0.0.1"
  },
  "dependencies": {
    "lucide-vue-next": "^0.548.0",
    "vue": "^3.5.17"
  },
  "devDependencies": {
    "@types/node": "^22.15.32",
    "@vitejs/plugin-vue": "^6.0.0",
    "typescript": "~5.8.0",
    "vite": "^7.0.0",
    "vue-tsc": "^2.2.10"
  }
}
